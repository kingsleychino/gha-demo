name: Or Condition Demo
on: push
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run for Nonso or Kingsley
        if: ${{ contains(fromJson('["Nonso", "Kingsley"]'), github.actor) }}
        run: echo "It is triggered by either Nonso or Kingsley"
