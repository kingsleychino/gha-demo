name: If Demo
on:
  push:
    branches:
      - main
      - develop
jobs:
  demo:
    runs-on: Run this step
    steps:
      - name: Run this step
        if: github.ref == 'refs/heads/main'
        run: echo "Running this step"
