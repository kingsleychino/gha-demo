name: Inputs Demo
on: 
  workflow_dispatch:
    inputs:
      environments:
        description: Chose the environment to deply
        required: true
        default: dev
jobs:
    inputs-demo:
      runs-on: ubuntu-latest
      steps:
        - name: Deply the App
          run: echo "Deploying the application to ${{ inputs.environments }}"
